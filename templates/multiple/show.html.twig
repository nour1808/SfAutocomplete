{% extends 'base.html.twig' %}

{% block title %}Multiple{% endblock %}

{% block body %}
    <h1>Multiple</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ multiple.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ multiple.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_multiple_index') }}" class='btn btn-info'>back to list</a>

    <a href="{{ path('app_multiple_edit', {'id': multiple.id}) }}" class='btn btn-primary'>edit</a>

    {{ include('multiple/_delete_form.html.twig') }}
{% endblock %}
