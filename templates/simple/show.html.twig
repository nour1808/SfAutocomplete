{% extends 'base.html.twig' %}

{% block title %}Simple{% endblock %}

{% block body %}
    <h1>Simple</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ simple.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ simple.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_simple_index') }}" class='btn btn-info'>back to list</a>

    <a href="{{ path('app_simple_edit', {'id': simple.id}) }}" class='btn btn-primary'>edit</a>

    {{ include('simple/_delete_form.html.twig') }}
{% endblock %}
